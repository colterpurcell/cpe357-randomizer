#include <stdio.h>
#include <stdlib.h>
#include "header.h"

/**
 * @brief a program that takes a list of numbers passed in from stdin and sorts them using a user specified
 * amount of threads. The program will then print the sorted list of numbers to the screen.
 * @param argv[1] the number of threads to use
 */

int main(int argc, char const *argv[])
{
    return 0;
}
